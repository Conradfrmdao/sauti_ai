import { AudioLines, MessageSquareText } from "lucide-react";

export function VoiceIcon() {
  return <AudioLines size={26} strokeWidth={2.1} />;
}

export function TextIcon() {
  return <MessageSquareText size={25} strokeWidth={2.1} />;
}
